name: custom_image_view_example
description: Example app for the custom_image_view package.
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  custom_image_view:
    path: ..
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/logo.svg
